---
name: landing-acceptance
description: Чек-лист приёмки ветки лендинга перед подключением домена. Использовать перед деплоем, перед PR из landings/*, при вопросе «готов ли лендинг к домену» и при финальной проверке ветки.
---

# Приёмка ветки

Лендинг готов к домену, только когда пройдены **все** пункты.
Не отмечайте пункт, не выполнив команду.

## Машинная часть

```bash
node tooling/validate-landing.mjs --slug <slug> --prod
node tooling/check-uniqueness.mjs
npm run build:landing -- --slug <slug> --prod
```

- [ ] конфиг проходит схему
- [ ] каждый файл из `config.media` лежит в `public/` ветки, `alt` заполнены
- [ ] `media.og` своя у этого домена, а не переиспользована с соседнего
- [ ] видео подключено id ролика, а не ссылкой на файлообменник
- [ ] разбор (`insight`) заполнен и не совпадает с другими ветками
- [ ] в Фонаре-оффере даты `validUntil` ещё не прошли, у цены и остатка есть `source`
- [ ] нет пунктов из `forbiddenClaims` и системных запретов
- [ ] title / H1 / FAQ уникальны
- [ ] `dist/` открывается без бэкенда

## Ручная часть

- [ ] квиз завершается маршрутом **без контакта**
- [ ] разъяснения без выдуманных норм
- [ ] видно, чем страница не портал
- [ ] 428px без горизонтального скролла
- [ ] один главный CTA, второй спокойный
- [ ] canonical, og, robots, sitemap смотрят только на свой домен
- [ ] секреты не в git

## Проверка в браузере

```bash
npx http-server dist -p 4321 -s &
```

Затем на 428px:

1. Фонарь-оффер показывает то, что реально подтвердил застройщик; счётчик,
   если включён, идёт к настоящей дате
2. пройти квиз до конца — маршрут показался, полей контакта не появилось
3. перезагрузить — состояние восстановилось
4. вернуться назад — ответы не потерялись
5. посчитать липкие CTA на мобиле: должен быть ровно один
6. проверить горизонтальный скролл: `document.documentElement.scrollWidth - clientWidth === 0`

Для автоматизации есть `superpowers-chrome` (навык `browsing`).

## Секреты

Ключи аналитики и пиксели в открытый репозиторий не коммитятся.
В конфиге только имя провайдера, в `.env.example` только имена переменных.

```bash
git grep -nE "(api[_-]?key|secret|token|G-[A-Z0-9]{8})" -- . ':!*.example'
```
