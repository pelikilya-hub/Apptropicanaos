# Активный лендинг для локальной сборки. По умолчанию берётся единственный
# каталог в /landings, иначе _example.
LANDING_SLUG=_example

# Ключи аналитики НИКОГДА не коммитятся. Только имена переменных.
PUBLIC_ANALYTICS_ID=
